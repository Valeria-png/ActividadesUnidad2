// const saveBtn = document.getElementById('saveBtn');
// const nameInput = document.getElementById('name');

// saveBtn.addEventListener('click') 
// {
// // alert(`Hola ${nameInput.value}`);
// const content = document.createElement('p');
// content.innerHTML = `Hola ${nameInput.value}`;
// document.body.appendChild(content);
// }



const num =0;  
// Es un número
let nom = "";
// una variable que puede cambiar con el tiempo
// var ya casi no se usa

const alumnos = [
    {nom: "yo", num:9},
    {nom: "yo", num:9}
]
// un arreglo

// Para hacer una función
function suma(a,b){
    // algoritmo a realizar
   const resultado = a+b;
//    se puede mandar a hacer un console log para imprimir desde la función y sólo mandar a llamar la función
   return resultado;
}
// a y b se refieren a los parámetros que entran a la función

// const suma = (a,b) 
// es igual a a+b, no siempre se puede mandar a llamar asi?

console.log(suma(1,2))
// para mandar a llamar

// async function es una función que espera la devolución de una respuesta para terminar de ejecutarse

